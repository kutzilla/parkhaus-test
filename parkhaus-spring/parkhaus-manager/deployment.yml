apiVersion: apps/v1
kind: Deployment
metadata:
  name: manager-spring-deployment
spec:
  selector:
    matchLabels:
      app: manager-spring
  replicas: 1
  template:
    metadata:
      labels:
        app: manager-spring
    spec:
      containers:
        - name: manager-spring
          image: registry-cloud.k8s.intern.viadee.de/dip/manager-spring:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          readinessProbe:
            httpGet:
              path: /parkhaus
              port: 8080
            failureThreshold: 7
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 1
      imagePullSecrets:
        - name: registry-cloud

---

apiVersion: v1
kind: Service
metadata:
  name: manager-spring-service
spec:
  selector:
    app: manager-spring
  ports:
    - port: 80
      targetPort: 8080

  type: ClusterIP

---

apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: manager-spring-ingress
spec:
  rules:
    - host: manager-spring.k8s.intern.viadee.de
      http:
        paths:
          - backend:
              serviceName: manager-spring-service
              servicePort: 80
            path: /
  tls:
    - secretName: tls-k8s-intern-viadee-de